# Backend — Node.js + Express

## Starten
```
npm install
npm start
```
Server draait op http://localhost:3001

## Vereisten
- Node.js geïnstalleerd
- XAMPP met MySQL draaiend
- `.env` bestand aangemaakt (kopieer `.env.example`)
